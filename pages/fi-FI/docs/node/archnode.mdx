# ArchNet-node

Moniydin ytimille perustuva ArchNet-solmupalvelin, muokattu V2bX:stä, tukee V2ay, Trojan, Shadowsocks, Tuic ja Hysteria -protokollia.

### Yhden napsautuksen asennus

```
wget -N https://raw.githubusercontent.com/archnets/node/master/scripts/install.sh && bash install.sh
```

## Rakentaminen

```bash
# Määritä -tags-vaihtoehdolla käännettävä ydin, valinnaiset xray, sing
go build -v -o ./node -tags "xray sing with_reality_server with_quic with_grpc with_utls with_wireguard with_acme" -trimpath -ldflags "-s -w -buildid="
```

import Giscus from '@/components/giscus';

<Giscus />
